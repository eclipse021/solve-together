# 후보키

- 문제: [프로그래머스 문제 바로가기](https://school.programmers.co.kr/learn/courses/30/lessons/42890)
- 날짜: 2026-07-24
- 플랫폼: PG
- 제한 시간: 40분

## 풀이 기록

| 이름 | 결과 | 풀이 시간 | 힌트/풀이 참고 |
| --- | --- | ---: | :---: |
| 준현 | 재풀이 필요 | 2시간 이상 | O (알고리즘) |
| 준희 | 성공 | - | △(구현 디테일만) |

결과는 `성공`, `미완료`, `재풀이 필요` 중 하나로 기록합니다.

## 짧은 회고

### 준현

- 접근:
  - 처음에는 컬럼 조합을 DFS로 생성하고, 각 조합마다 후보키 판정 로직 실행했다.
  - 작은 조합이 후보키가 되면 그 이후의 큰 조합은 자연스럽게 걸러질 것이라고 생각했기 때문
- 막힌 지점:
  - DFS가 조합의 크기순이 아니라 "하나의 경로"를 깊게 탐색해, key 개수가 작은 순이 아닌 방식으로 조합이 만들어짐을 놓침,,, (차라리 BFS 조합 방식을 써야 하나 고민함)
  - 기존 후보키가 현재 조합에 포함되는지 검사하는 방법도 생각 못함
- 다음에 기억할 것:
  - DFS는 한 경로를 깊이 탐색하고 BFS는 같은 깊이를 넓게 탐색하지만, 목표 조합 크기별로 DFS를 반복하면 작은 조합부터 탐색할 수 있다.
  - 정렬된 두 범위의 포함 관계는 includes(A, B)를 통해 B가 A의 부분집합인지 검사할 수 있다.

### 준희

- 접근: "모든 컬럼 조합 완전탐색 + set으로 유일성/최소성 판정"이라는 뼈대와 흐름은 스스로 다 잡음. 컬럼 조합을 크기 오름차순으로 만들어 유일성(각 조합으로 만든 튜플 집합 크기 == 행 개수) 검사 후, 기존 후보키의 부분집합이면 제외하는 방식으로 최소성 처리.
- 막힌 지점: 처음에 컬럼 인덱스가 아니라 실제 값 리스트를 set에 담으려다 막힘 → 인덱스로 다뤄야 한다는 것. 그리고 부분집합 판정 `issubset`, 조합을 리스트에 펼쳐 담는 `extend`의 존재를 잊고 있어서 그 세 가지를 힌트로 받음. 전체 로직이 아니라 디테일 도움.
- 다음에 기억할 것: 값 자체 대신 인덱스로 조합/집합을 다루기(값은 unhashable·중복 이슈, 인덱스는 깔끔). `set.issubset()`으로 부분집합 판정 → 최소성 검사에 유용. `list.extend()`는 iterable 내용물만 펼쳐 담음(vs `append`). 후보키 = 유일성 + 최소성, 둘 다 만족해야 함.
